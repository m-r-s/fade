function generate(target_dir, samples, seed)

mkdir(target_dir);
classes = {0 1};

% Tone-in-noise detection
funname = 'TIN';
offsets = -18:3:21;
generate_conditions(target_dir, funname, classes, ...
  num2cell(66+offsets), samples, {0.005}, seed);
generate_conditions(target_dir, funname, classes, ...
  num2cell(57+offsets), samples, {0.015}, seed);
generate_conditions(target_dir, funname, classes, ...
  num2cell(57+offsets), samples, {0.020}, seed);
generate_conditions(target_dir, funname, classes, ...
  num2cell(54+offsets), samples, {0.035}, seed);
generate_conditions(target_dir, funname, classes, ...
  num2cell(54+offsets), samples, {0.050}, seed);
generate_conditions(target_dir, funname, classes, ...
  num2cell(51+offsets), samples, {0.100}, seed);
generate_conditions(target_dir, funname, classes, ...
  num2cell(48+offsets), samples, {0.200}, seed);

%% Spectral masking Tone/Noise-in-Noise
%funname = 'SM';
%offsets = -18:3:21;
%generate_conditions(target_dir, funname, classes, ...
%  num2cell(6+offsets), samples, {'45,750'}, seed);
%generate_conditions(target_dir, funname, classes, ...
%  num2cell(21+offsets), samples, {'45,900'}, seed);
%generate_conditions(target_dir, funname, classes, ...
%  num2cell(39+offsets), samples, {'45,1000'}, seed);
%generate_conditions(target_dir, funname, classes, ...
%  num2cell(21+offsets), samples, {'45,1100'}, seed);
%generate_conditions(target_dir, funname, classes, ...
%  num2cell(6+offsets), samples, {'45,1250'}, seed);
%generate_conditions(target_dir, funname, classes, ...
%  num2cell(3+offsets), samples, {'45,1500'}, seed);
%generate_conditions(target_dir, funname, classes, ...

end
